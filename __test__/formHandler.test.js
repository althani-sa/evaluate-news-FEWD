import {handleSubmit} from "../src/client/js/formHandler"
describe("async formHandler eventlistener that gets data from AYLIEN TEXT API", () => {
    test("Function is defined", () => {
     expect(handleSubmit).toBeDefined()
    })
})